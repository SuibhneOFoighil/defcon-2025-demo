#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Husky pre-commit hook running!"

# Now includes full typecheck including Storybook stories since all TypeScript errors are fixed
bun run lint && bun run test && bun run typecheck